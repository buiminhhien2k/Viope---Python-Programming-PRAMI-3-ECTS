def main():
	while True:
		string = input("Write something (quit ends): ")
		if string == "quit":
			break
		elif string != "quit" and len(string) <10:
			print("Too short")
		else:
			print(string)
			define(string)
def define(string):
	if "X" in string:
		print("X spotted!")
	else:
		pass

if __name__ == "__main__":
	main()
